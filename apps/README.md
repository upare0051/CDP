# Apps Workspace (Scaffold)

This workspace introduces a safe, incremental split between:

- `website` (marketing + private website admin)
- `product` (customer-facing deployable platform)

Current production code remains in existing `frontend/` and `backend/` folders until migration cutover is complete.
